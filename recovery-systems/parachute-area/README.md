# Archived Prototype

This directory contains the earlier standalone parachute sizing prototype.

Status:

- reference only
- not the active application path
- retained for historical context and formula/UI comparison

Active implementation:

- DRIFT lives under `src/drift/`

Do not extend `parachute_area.py` or `parachute_gui.py` as the primary app. New recovery-tool development should target DRIFT instead.
